# User-Dashboard
Implemented dynamic rendering using Handlebars.js. Created a template that iterates over a JSON dataset of users and applies conditional logic to highlight admin roles. Used partials for reusable sections and integrate with Tailwind CSS for styling.
